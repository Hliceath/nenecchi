# Nenecchi

⚠️ Disgusting code

Start quickly a c/c++ project with `nenecchi new <project_name>` which sets up four directories `bin/ build/ include/ source/` and three files `source/main.c Makefile .clang-format`
